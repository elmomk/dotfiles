---
icon: lucide/book-open
---

# __SITE_NAME__

<!-- House overview pattern. Replace the placeholders, keep the shape:
     gist → big-picture diagram → what's covered → links. -->

!!! abstract "The one-paragraph version"
    REPLACE ME — the whole topic in a nutshell, before any detail, so the reader has a
    frame to hang everything on. If they read only this callout, they should still walk
    away with the shape.

## The big picture

```mermaid
flowchart LR
    A[REPLACE] --> B[with a diagram of] --> C[the flow / architecture]
```

## What's covered

<!-- Linkify each page (e.g. [The problem](the-problem.md)) AS you create it under docs/
     and add it to `nav` in zensical.toml. Plain text here keeps a fresh scaffold building
     clean (no links to pages that don't exist yet). -->

| Page | What you'll get |
| --- | --- |
| `the-problem.md` | Why this exists / what breaks without it |
| `how-it-works.md` | The mechanism, step by step |
| `walkthrough.md` | One real case traced through with values |
| `mental-model.md` | The one-sentence takeaway |

!!! tip "Start here"
    Read in order, or jump straight to the worked example if you learn best from a
    concrete trace.
