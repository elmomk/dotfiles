# Open URL in Browser

Open a URL in the default Windows browser from WSL.

## Usage

`/open <url>`

## Instructions

Run `explorer.exe "$ARGUMENTS"` to open the provided URL in the default Windows browser. If no arguments are provided, ask the user for the URL.
