#!/usr/bin/env bash

SESSION=$(tmux list-sessions | cut -d: -f1 |fzf --preview "tmux capture-pane -ep -t {}")

if ! env | grep -i tmux >/dev/null; then
    tmux attach -t "$SESSION"
else
    tmux switch -t "$SESSION"
fi
